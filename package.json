{
	"name": "parcel-web-template",
	"version": "1.0.0",
	"scripts": {
		"clean": "rimraf dist",
		"dev": "npm run clean && parcel ./src/*.html",
		"build": "npm run clean && parcel build ./src/*.html --no-source-maps --public-url ."
	},
	"author": "Bakhriev",
	"license": "ISC",
	"description": "Parcel web template",
	"devDependencies": {
		"@agragregra/parcel-namer-beautify": "^1.0.1",
		"@parcel/transformer-sass": "^2.12.0",
		"parcel": "^2.12.0",
		"posthtml-include": "^2.0.1",
		"prettier": "^3.3.3",
		"rimraf": "^6.0.1",
		"sharp": "^0.31.3"
	}
}
